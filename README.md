# python-repo-template

Extract code snippets from a markdown file and upload them to Gist.

For this to work, you will need a Github Personal Access Token.
Put it in a `.env` file like this `GITHUB_GIST_TOKEN=<your_token>`